# For use with Helmsman

helmRepos:
  loki: "https://grafana.github.io/loki/charts"

namespaces:
  loki:

apps:
  promtail:
    chart: loki/promtail
    version: 0.16.0
    enabled: true
    namespace: loki
    priority: -2
    valuesFiles:
     - values-promtail.yaml
